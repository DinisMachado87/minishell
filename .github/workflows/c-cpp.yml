name: C CI

on:
  push:
    branches: [ "unity_testing" ]
  pull_request:
    branches: [ "unity_testing" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout all the submodules
      uses: actions/checkout@v4
      with:
        submodules: recursive
      #    - name: configure
      #      run: ./configure
    - name: make
      run: make
        #    - name: make check
        #      run: make check
        #    - name: make distcheck
        #      run: make distcheck

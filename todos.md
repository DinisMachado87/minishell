DINIS

- when quotes without spaces after the word store as the same word

JUSTIN

- echo prints space bettween words

==AND NODE==

left = regular left args
right = NULL
instead of right we have:
args[0] = "string with the remaining string after the '&&'"

export env=cat && echo hello | tr a-z A-Z && ls -l

type = AND
left = type: CMD :: args: export env=cat
right = NULL
intead of right we have:
args: "echo hello | tr a-z A-Z && ls -l"

$env hello$carhithere$hhhko
e n e n e n

'ls -l'
'yellow'
'ok'

hello
world
helloworld
hello
helloworldhello
world\n
helloworldhelloworld

hello
world
hello
world\n
helloworldhellowold

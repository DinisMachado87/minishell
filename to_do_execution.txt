	- Check if command was found, if not print (cmd_name: command not found)
	- Built-in commands
		- cd
		- pwd
		- export
		- unset
		- env
		- exit
	- Redirects
	- && + ||
	- Replace all the strcpy, strcat, strlen, etc. functions with own
	- Do we need a proper exit code?
	/*
	int status;
    waitpid(pid, &status, 0);

    if (WIFEXITED(status))
        return WEXITSTATUS(status); // Pass child exit code
    else
        return 1; // Abnormal exit
	*/
	- Figure out advantage of global variable for signal handling
	- Figure out what those functions are usefull for ( tcsetattr, tcgetattr, tgetent, tgetflag, tgetnum, tgetstr, tgoto, tputs)
